/*Basic Math Function:
Write a function called multiply that takes two numbers and returns their product.
Test it with multiply(4, 5) and log the result (should be 20).*/

let multiply = function (num1, num2) {
  return num1 * num2;
};
console.log(multiply(4, 5));
