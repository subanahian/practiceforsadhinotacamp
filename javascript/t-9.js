/*Create a function subtract that takes two numbers and returns their difference.
Use it in a sentence, e.g., console.log("10 minus 4 is " + subtract(10, 4));.*/

function subtract(a, b) {
  return a - b;
}
console.log("10 minus 4 is " + subtract(10, 4));
